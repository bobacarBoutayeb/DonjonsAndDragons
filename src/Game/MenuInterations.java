/*package Game;

public class MenuInterations {
    private Game game;
    public MenuInterations(){
        this.game = new Game();
    }

    public void playerStats(){
        switch (this.menu.playerStatsMenu()){
            case "1" -> this.menu.showName(this.player);
            case "2" -> this.menu.showHealth(this.player);
            case "3" -> this.menu.showAttack(this.player);
            case "4" -> this.menu.mainMenu();
        };
    }

    public void playerModifyStats(){
        switch (this.menu.playerStatsMenuModify()){
            case "1" -> this.player.setName(this.menu.getPlayerName());
            case "2" -> this.player.setHealth(this.menu.setHealthPrompted());
            case "3" -> this.player.setAttack(this.menu.setAttackPrompted());
            case "4" -> this.menu.mainMenu();
        };
    }
}
*/